fastapi==0.116.1
httpx==0.28.1
pydantic-settings==2.10.1
uvicorn[standard]==0.35.0
pytest==8.4.1
ruff==0.12.10
black==25.1.0
pytest-asyncio==1.2.0
# Keep the v1 SDK API used by the backend, but support the current
# handshake-era MCP protocol revisions advertised by Registry remotes.
# 1.9.4 predates those revisions and can fail during `initialize`.
mcp>=1.29.1,<2
starlette==0.47.2
sse-starlette==2.4.1
cryptography==45.0.0
docker==7.1.0
